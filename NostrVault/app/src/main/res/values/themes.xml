<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Base theme for splash/startup before Compose takes over.
        The actual theming is handled entirely by Jetpack Compose (NostrVaultTheme).
    -->
    <style name="Theme.NostrVault" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#FF141419</item>
        <item name="android:statusBarColor">#FF141419</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
</resources>
