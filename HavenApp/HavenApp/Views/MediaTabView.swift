import SwiftUI

// MARK: - MediaTabView

/// Blossom tab that displays local and mirrored media via MediaGalleryView.
struct MediaTabView: View {
    var body: some View {
        MediaGalleryView()
    }
}
